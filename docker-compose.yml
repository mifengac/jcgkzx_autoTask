services:
  autotask:
    image: jcgkzx-autotask:offline-20260310-jsonfix
    container_name: jcgkzx-autotask
    restart: unless-stopped
    env_file:
      - ${ENV_FILE:-.env}
    ports:
      - "${APP_PORT:-8000}:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./uploads:/app/uploads
