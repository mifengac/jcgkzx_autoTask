# 任务:我要调整项目逻辑,你帮我设计下
## 1. 这个项目现在主要有2个功能:
1. 对数据进行监测然后发短信(通过向oracle11g插入数据)
2. 执行定时脚本向人大金仓V8数据库插入数据
### 1. 我想调整一下功能1,对数据进行监测然后发短信的逻辑,调整为"主题监测"和"自定义监测"
1. 主题监测:就是数据都从同一个地方出来,可以是接口请求,也可以是从数据库某个表或某个SQL出来,解决的问题是同一接口多次请求,对服务器压力比较大,比如jcgkzx_autoTask\0310jsbrjq_monitor.py和jcgkzx_autoTask\monitor_wcnr_jq.py现在都是从"http://68.253.2.111/dsjfx/case/list"接口拿的数据,只不过参数不同,所以可以合并,并且之后可能会增加更多这个接口的数据监测,如果每次都新增一个脚本,每次都要多请求一次接口,比较浪费资源,你帮我设计下
2. 自定义监测:就是跟之前一样自己写一个脚本,然后配参数和环境变量
## 2. 注意事项:
1. 接口都是在内网请求的所以当前互联网环境无法测试
2. 到时我想将这个项目打包为docker部署到ubuntu22,项目的主要目的是方便我配置,不用每次那么麻烦
3. 如果有更好的意见可以向我提出
---
# 任务: 调整项目监测逻辑，设计"主题监测"架构

## 背景
项目现有2个功能：
1. 数据监测 → 发短信（向 Oracle 11g 的 yfgadb.dfsdl 表插入数据）
2. 定时脚本 → 向人大金仓V8数据库插入数据

## 需求：改造功能1

### 现状问题
monitor_wcnr_jq.py 和 0310jsbrjq_monitor.py 都从同一个接口
http://68.253.2.111/dsjfx/case/list 拿数据，只是查询参数不同。
每新增一种监测就新建一个脚本、多请求一次接口，浪费资源。

### 目标设计
分为两种模式：
1. 主题监测：多个监测规则共用一次数据请求。新增规则只需配置
   （数据源+过滤条件+短信模板+接收人），不写新脚本。
2. 自定义监测：保持现有模式，自己写脚本 + 配环境变量。

### 关键约束
- 合并后多主题共用 Oracle 去重表，需保证主题间去重互不干扰
- 接口都在内网，当前环境无法测试真实请求
- 最终打包为 Docker 部署到 Ubuntu 22，规则通过配置文件注入，
  不需要改代码重新打包镜像
- 调度需统一入口，所有主题共用一个定时器，一次请求分发多规则

### 输出要求
1. 先输出设计方案（模块划分 + 数据流 + 配置文件格式示例）
2. 我确认后再开始写代码
3. 如果有更优架构建议，一并提出